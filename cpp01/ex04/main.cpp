#include <iostream>
#include <fstream>
int main(int ac ,char **av)
{
    int i;
    i = 0;
    std::string line;
    std::fstream file(av[1],std::ios::binary );

    file.is_open();
    {
       
    }
}